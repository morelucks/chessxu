name: Test & Coverage

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    name: Test & Upload Coverage
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Compile test sources
        run: tsc -p tsconfig.test.json

      - name: Run tests with coverage (c8 + lcov)
        run: npm run test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          flags: sdk
          name: chessxu-sdk
          fail_ci_if_error: true
          verbose: true

      - name: Enforce coverage threshold
        run: npm run coverage:check

  # Separate job so the coverage gate is a distinct required status check
  coverage-gate:
    name: Coverage Gate (>= 80%)
    runs-on: ubuntu-latest
    needs: test
    if: always()
    steps:
      - name: Check coverage gate result
        run: |
          if [ "${{ needs.test.result }}" != "success" ]; then
            echo "Coverage gate failed — test job did not succeed."
            exit 1
          fi
          echo "Coverage gate passed."
