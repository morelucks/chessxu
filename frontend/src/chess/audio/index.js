/**
 * Audio module barrel export.
 *
 * Re‑exports the public API from SoundEngine so consumers can import
 * from `audio/` directly.
 */
export { playChessSound, initAudioContext } from './SoundEngine';
